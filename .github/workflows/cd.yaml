name: Glossing Entries Deploy
on: 
  pull_request:
      branches:
        18-constant-props
jobs:
    deploy:
      strategy: 
        matrix:
            machines: [vlcdhp02]
      runs-on: ${{ matrix.machines }}
      steps:
        - uses: actions/checkout@v3
        - run: |
            cd /opt/tomcat/webapps/glossing/
            git stash
            git checkout 18-constant-props
            git pull
            git checkout $GITHUB_REF_NAME
            git pull
            git merge 18-constant-props
